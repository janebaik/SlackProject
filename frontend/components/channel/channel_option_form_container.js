import { connect } from 'react-redux';
import ChannelOptionForm from './channel_option_form';

const msp = (state, ownProps) => {

    return {
    }
}
const mdp = (dispatch) => {
    return {
    }
}

export default connect(msp, mdp)(ChannelOptionForm)